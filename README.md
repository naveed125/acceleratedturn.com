# acceleratedturn.com website

This is site is built using github pages and uses the tactile jekyll theme. For that matter it needs to be public repo.

## Local setup

Ensure you have "bundle" installed

```
gem install jekyll bundler
bundle install
```

Run local site

```
bundle exec jekyll serve --livereload
```



